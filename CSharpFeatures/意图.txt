测试一下.net 程序集加载的基本策略
环境:
1.通过Ioc创建接口的实现类实例,这个过程可以通过多种方式:
	1)反射加载
	2)MEF
	3)EntLib
	4)...
2.被加载的程序集(A)需要引用另一个程序集(B),而且这两个都不被客户代码所在的(C)直接引用
3.A,B放在C不能直接访问的目录下
4.验证:
	1)加载A时是否能自动找到B
	2)如果引用关系中有DLL注册到GAC的情况是否会打断查询目录的规则